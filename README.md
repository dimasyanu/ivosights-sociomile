# Ivosights Sociomile (Test Project)

# Installation
1. Install task runner. (See: [https://taskfile.dev/docs/installation](https://taskfile.dev/docs/installation))
2. Run the default task in Taskfile.yml (Simply run `task`/`go-task`).